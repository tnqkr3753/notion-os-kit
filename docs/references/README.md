# References

This directory is reserved for local, source-grounded reference notes.

Reference notes may contain live Notion page IDs, database IDs, view IDs, audit
results, or workspace-specific evidence. They are ignored by default for
open-source publishing.

Commit sanitized references only when they do not expose private workspace
state.

